spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cadmus
    username: ${DB_USER}
    password: ${DB_PWD}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    locations: db/migration/postgresql